import { Dimensions } from "react-native";

const { height, width } = Dimensions.get("screen");
import {
  moderateScale,
  moderateVerticalScale,
} from "react-native-size-matters";

export { height, width, moderateScale, moderateVerticalScale };
